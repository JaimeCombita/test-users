springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
  info:
    title: User Management API
    description: API RESTfull para gestión de usuarios y teléfonos
    version: 1.0.0

jwt:
  secret: MiClaveSuperSecretaDeAlMenos32Caracteres123!
  expiration: 1h
  refresh: 7d

app:
  admin:
    email: leonardo.102408@gmail.com
    password: admin123

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus
  endpoint:
    health:
      show-details: always
  tracing:
    enabled: false
    sampling:
      probability: 1.0   # 100% de requests trazados (en producción puedes bajar este valor)

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  mail:
    port: 587
    host: smtp.gmail.com
    username: leonardo.102408@gmail.com
    password: rndp sahj dkdn jpxs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Ensure the application binds to the PORT provided by the environment (Cloud Run sets PORT)
server:
  port: ${PORT:8080}

